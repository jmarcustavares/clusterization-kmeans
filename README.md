# clusterization-kmeans
> Clusters Brazilian schools using K-Means (k=5) based on size, academic performance, physical infrastructure index, and average tuition. Built with a scikit-learn pipeline featuring One-Hot Encoding for categorical variables, the model outputs a labeled Excel file with per-cluster summaries.  
